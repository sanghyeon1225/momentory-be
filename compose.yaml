services:
  postgres:
    image: postgres:17-alpine
    container_name: momentory-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-momentory}
      POSTGRES_USER: ${POSTGRES_USER:-momentory}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-momentory}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  postgres-data:

